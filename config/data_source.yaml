{
  "provider": "yixin",
  "mock": {
    "seed": 20260622,
    "description": "确定性离线样例数据。每个日期会生成一致的行情，便于测试整个 Loop。"
  },
  "file": {
    "snapshot_pattern": "data/market_snapshot/{date}.json"
  },
  "yixin": {
    "skill_script": "scripts/run_yixin_stock_workflow.py",
    "skip_image": true,
    "notes": "优先调用仓库内的 Yixin Skill 脚本，原始响应落入 data/raw/yixin/<date>/。API Key 只从环境变量或 ~/.config/yixin-api/api-keys.json 读取。"
  }
}
